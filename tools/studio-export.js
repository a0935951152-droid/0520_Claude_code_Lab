/* ============================================================
   Design Studio · export module
   v0.13.0 — split from studio.js to comply with 1000-line rule
   Depends on: studio.js (state, STUDIO_VERSION, defaultTokensFlat, toast)
   ============================================================ */
'use strict';

function buildPatch() {
    const defaults = defaultTokensFlat();
    const tokens = {};
    const tokensDark = {};
    for (const [k, v] of Object.entries(state.tokens.light)) {
        if (defaults.light[k].toLowerCase() !== v.toLowerCase()) tokens[k] = v;
    }
    for (const [k, v] of Object.entries(state.tokens.dark)) {
        if (defaults.dark[k].toLowerCase() !== v.toLowerCase()) tokensDark[k] = v;
    }
    return {
        version: 'studio_patch_v3',
        createdAt: new Date().toISOString(),
        basedOn: { studioVersion: STUDIO_VERSION },
        themePreset: state.currentPreset,
        tokens,
        tokensDark,
        textPatches: { ...state.textPatches },
        movePatches: { ...state.movePatches },
        panelLayout: state.panelLayout ? structuredClone(state.panelLayout) : null,
        elementOverrides: [],
        htmlPatches: []
    };
}

function exportTokensCSS() {
    const lightLines = Object.entries(state.tokens.light).map(([k, v]) => `    ${k}: ${v};`).join('\n');
    const darkLines = Object.entries(state.tokens.dark).map(([k, v]) => `    ${k}: ${v};`).join('\n');
    const css = `/* Generated by Design Studio · ${new Date().toISOString()} */
/* Theme preset: ${state.currentPreset} */
/* Studio version: ${STUDIO_VERSION} */

:root {
${lightLines}
}

body.dark-mode {
${darkLines}
}
`;
    const blob = new Blob([css], { type: 'text/css' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `tokens-${state.currentPreset}-${Date.now()}.css`;
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
    toast('✓ tokens.css 已下載');
}

async function exportPatch() {
    const patch = buildPatch();
    const isEmpty = Object.keys(patch.tokens).length === 0
        && Object.keys(patch.tokensDark).length === 0
        && Object.keys(patch.textPatches).length === 0
        && Object.keys(patch.movePatches).length === 0
        && !patch.panelLayout;
    if (isEmpty) {
        toast('沒有修改可匯出');
        return;
    }
    const json = JSON.stringify(patch, null, 2);
    const blob = new Blob([json], { type: 'application/json' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `studio-patch-${Date.now()}.json`;
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
    let copied = false;
    try {
        await navigator.clipboard?.writeText(json);
        copied = true;
    } catch (e) {}
    toast(copied ? '✓ Patch 已下載 + 複製剪貼簿' : '✓ Patch 已下載');
}
